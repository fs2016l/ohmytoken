/**
 * Persisted scanner contract revision.
 *
 * Stable ID transitions are reconciled inside the 24-hour overlap window, so an
 * existing complete baseline remains valid and does not require an automatic full scan.
 */
export const SCANNER_REVISION = 2
